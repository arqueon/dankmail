import QtQuick

// Main triage window: unified thread list across accounts (chronological,
// per-account color indicator), quick filters (unread / starred / account),
// per-thread actions (read, star, archive, trash, snooze, open in web),
// plain-text preview pane with quick-reply underneath (Ctrl+Enter sends).
// TODO(anillo1): list + preview + actions; quick reply is Anillo 2.
Item {
    id: triagePopup
}
