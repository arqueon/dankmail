import QtQuick

// Settings: accounts (add/remove, embedded Gmail OAuth flow, IMAP/SMTP
// fields), NotifyRules, DND windows, chained-action policies, retention,
// polling interval, autostart. TODO(anillo1: accounts; anillo2: the rest).
Item {
    id: settingsModal
}
